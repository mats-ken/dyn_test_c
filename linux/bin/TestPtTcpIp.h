#define	GPIO4OUT(X)			do{	\
	\
} while(0)

#define	TEST_PT_IF(X)		do{} while(0)
#define	TEST_PT_FOR(X)		do{} while(0)
#define	TEST_PT_WHILE(X)	do{} while(0)
#define	TEST_PT_DO(X)		do{} while(0)
#define	TEST_PT_SWITCH(X)	do{} while(0)


static	int		EXECNT = 0;
#define	C1ZERO	0
